            - ชื่อ: อัปเดตไฟล์ Json
  # คุณสามารถปักหมุดการคอมมิตหรือเวอร์ชันที่แน่นอนได้
  # ใช้: restackio/update-json-file-action@6f50afee9a03a456a30cd574123db793319f7544
  ใช้: restackio/update-json-file-action@2.1
  กับ:
    # ชื่อไฟล์/เส้นทางที่จะแก้ไข เช่น 'package.json'
    ไฟล์:
    #ช่องที่ต้องแก้ไข สามารถซ้อนกันได้ ตัวอย่าง: {'abc[0]': 'value_here', 'zx': 'value_here'}
    ฟิลด์:
          
